# API Contracts

This directory contains machine-readable PalCenter Companion API contracts.

- [openapi.yaml](openapi.yaml) defines the initial HTTP discovery interface.

The contracts are architectural artifacts in v0.1.0. No HTTP or gameplay service is implemented. The API version (`v1`) evolves independently from the Companion application version (`0.1.0`).
