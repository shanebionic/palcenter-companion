# Future Architecture Boundaries

Future milestones may add separate internal event-bus and gameplay-hook modules here or in dedicated source directories. They must not be coupled to HTTP routing or the UE4SS lifecycle adapter.

No event bus, gameplay hook, player data, telemetry collection, or WebSocket implementation exists in v0.1.0.
