{
  "name": "palcenter-companion",
  "version": "0.3.1",
  "private": true,
  "description": "Optional in-process server extension that augments PalCenter with authoritative Palworld information.",
  "license": "MIT",
  "scripts": {
    "format:check": "node scripts/check-format.mjs",
    "lint": "node scripts/validate-content.mjs",
    "build": "node scripts/validate-structure.mjs",
    "docs:check": "node scripts/validate-docs.mjs",
    "validate": "npm run format:check && npm run lint && npm run build && npm run docs:check"
  },
  "engines": {
    "node": ">=22"
  }
}
